"use strict";
exports.__esModule = true;
exports.pointCellTextRect = exports.pointCellIconRect = void 0;
function pointCellIconRect(node) {
    // node.iconRect = new Rect(0, 0, 0, 0)
}
exports.pointCellIconRect = pointCellIconRect;
function pointCellTextRect(node) {
    // const x = node.rect.x || 0
    // const y = node.rect.y || 0
    // const w = node.rect.width
    // const h = node.rect.height
    // node.textRect = new Rect(x, y, w, h)
    // node.fullTextRect = node.textRect
}
exports.pointCellTextRect = pointCellTextRect;
